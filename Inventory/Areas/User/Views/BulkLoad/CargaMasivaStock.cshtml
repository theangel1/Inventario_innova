@{
    ViewData["Title"] = "Carga Masiva de Stock";
}
<div class="container-fluid">
    <h3>@ViewData["Title"]</h3>
    <hr class="bg-warning" />
    <p>En esta sección podrá cargar un archivo excel con múltiples productos para agregarlos al stock.</p>
    <div class="alert alert-warning">
        El archivo excel debe estar asociado solo a una empresa, y no pueden repetirse los sku de los productos.
        <p>Ver el siguiente ejemplo:</p>
        <img src="~/resources/example.png" />
    </div>

    <a class="btn btn-dark" href="~/resources/FormatoMovimiento.xlsx" download>
        Descargar excel de ejemplo.
    </a>

    <hr />

    <form method="post" enctype="multipart/form-data" class="form-text">

        <div class="form-group">
            <input type="file" class="form-control-file" name="files" />
        </div>

        <div class="form-group">
            <input type="submit" class="btn btn-dark" value="Cargar" />
            <a asp-action="Index" class="btn btn-warning">Volver</a>
        </div>
    </form>
</div>

@section Scripts{
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial");}



    @if (!string.IsNullOrEmpty(ViewBag.Message))
    {
        <script type="text/javascript">
          Swal.fire("¡Información!", '@ViewBag.Message', "info");
        </script>
    }
    @if (!string.IsNullOrEmpty(ViewBag.MessageSuccess))
    {
        <script type="text/javascript">
          Swal.fire("¡Información!", '@ViewBag.MessageSuccess', "success");
        </script>
    }

    


}